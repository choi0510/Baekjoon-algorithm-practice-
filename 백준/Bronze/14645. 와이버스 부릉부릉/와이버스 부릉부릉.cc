#include <iostream>
using namespace std;

int main() {

	ios::sync_with_stdio(false);
	cin.tie(0);

	int N, K;
	int A, B;
	cin >> N >> K;

	for (int i = 0; i < N; i++) {
		cin >> A >> B;
	}

	cout << "비와이";
}